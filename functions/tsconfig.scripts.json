{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // `paths` sans `baseUrl` : les chemins sont relatifs à ce fichier.
    "paths": {
      "@dimanche-batch/shared": ["../packages/shared/src/index.ts"]
    }
  },
  "include": ["scripts/**/*.ts"]
}
